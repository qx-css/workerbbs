# 2026-08-18 工作日志

## 社区风格主页 + 社交 + 深色模式（WorkerBBS）
- 后端 `src/index.ts`：新增 `POST /api/threads/:id/like`（点赞切换，作者/点赞者各加经验），
  `GET /api/threads/:id` 富化返回 `likes` / `liked`（当前用户）。关注/统计接口与 db 函数在前一日已落地。
- 前端 `public/app.js`：
  - `avatarHTML` 增加 `data-user` + `clickable` 类；`content` 全局委托点击头像/用户名跳 `#/user/:username`（stopPropagation 避免触发帖子卡片跳转）。
  - `viewUser` / `viewProfile` 重做为社区风格：封面横幅(用 bg_image)、重叠大头像、用户名+等级、粉丝/关注/获赞统计行、他人显示关注按钮(切换)、自己显示换头像/换封面/编辑资料。
  - `viewThread` 增加点赞栏，调 `/api/threads/:id/like`。
  - `viewSettings`：所有用户可见「个人偏好」含深色模式开关（localStorage `theme`）；管理员面板追加在下方。
- `public/index.html`：引入语义变量 `--text/--text-2/--bg/--surface/--border/--hover`，主体文字/背景改用语义变量；新增
  `.profile/.profile-cover/.profile-top/.profile-stats/.pstat/.follow-btn/.like-bar/.like-btn/.switch/.sec-title` 及 `[data-theme="dark"]` 深色覆盖。
- `migrations/0004_social.sql`（follows/likes 表+索引）此前已建，本次一并纳入提交。
- 验证：`node --check public/app.js` 通过；`npm run typecheck` 通过（exit 0）。
- 仍未 push（按约定部署由用户触发）。登录态修复 `5663b18` 也仍待用户 push 上线。

## 待办（用户之前提过、本次未做）
- `/admin` 独立路由 + 横屏后台布局（当前后台仍在 `#/settings` 内）。
- 本机 C: 盘满（Temp ~2.4G + npm-cache），挡本地 `wrangler dev`；GitHub Action 在云端不受影响。
